---
"@yggdrasil-forge/core": minor
---

Add ResourceManager: immutable economy management (canAfford, atomic applyCost, refund with refundable/refundPercent/max handling, per-tier and cumulative cost calculation). Also adds missing test coverage for CycleDetector cycle normalization (DT-4).

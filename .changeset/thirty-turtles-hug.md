---
"@yggdrasil-forge/core": minor
---

Add ChangeTracker: pure analyzer of TreeChange[] arrays. Produces selective cache invalidation, affected node IDs, internal conflict detection (duplicates, add_then_remove, rename chains, etc.), and rename mappings.
